工作区到暂存区用add .
暂存区到版本区用commit
工作区直接到版本区用commit -a -m"注释"；
git status：如果红色证明在工作区。如果绿色证明在暂存区。没有提示证明在版本区。
git log :可以看看ID来查看版本
git rm:删除工作区，并且表面上删除暂存区。如果想在版本区也删除。还得提交一次。
git rm -f:删除工作区和暂存区。版本区也删除了。如果想恢复
git reset --hard "ID名"